# Xscriptor Guake Themes

<p align="center">
  <img src="https://raw.githubusercontent.com/xscriptor-colors/terminal/main/previews/guake/preview.jpg" alt="Preview" width="900"/>
</p>

## Files

<ul>
  <li><code>themes/*.xml</code>: Color scheme files for Guake terminal, importable via the GUI.</li>
</ul>

## Installation

<p>Open Guake → Preferences (<kbd>F12</kbd> then right-click → Preferences) → Colors tab → Import.</p>

<p>Select the desired <code>.xml</code> theme file from the <code>themes/</code> directory.</p>

<p>Alternatively, copy the files manually:</p>

<pre><code>cp themes/x.xml ~/.config/guake/schemes/</code></pre>

<p>Then select it from the Colors dropdown in Preferences.</p>

## Themes

<ul>
  <li><code>x.xml</code>, <code>madrid.xml</code>, <code>lahabana.xml</code>, <code>miami.xml</code>, <code>paris.xml</code>, <code>tokio.xml</code>, <code>oslo.xml</code>, <code>helsinki.xml</code>, <code>berlin.xml</code>, <code>london.xml</code>, <code>praha.xml</code>, <code>bogota.xml</code></li>
</ul>
