# Xscriptor Wave Terminal Themes

## Files

<ul>
  <li><code>themes/*.json</code>: Theme definitions for Wave Terminal.</li>
</ul>

## Installation

<p>Wave Terminal uses a single <code>termthemes.json</code> file located at <code>~/.waveterm/termthemes.json</code>.</p>

<p>Merge the desired theme into your existing <code>termthemes.json</code>. Example structure:</p>

<pre><code>{
  "x": {
    "display:name": "Xscriptor x",
    "display:order": 1,
    "background": "#050505",
    "foreground": "#f7f1ff",
    "black": "#0a0a0a",
    "red": "#fc618d",
    ...
  }
}</code></pre>

<p>After adding the theme, select it from the terminal block header right-click menu, or set it in <code>settings.json</code>:</p>

<pre><code>"term:theme": "x"</code></pre>

## Themes

<ul>
  <li><code>x.json</code>, <code>madrid.json</code>, <code>lahabana.json</code>, <code>miami.json</code>, <code>paris.json</code>, <code>tokio.json</code>, <code>oslo.json</code>, <code>helsinki.json</code>, <code>berlin.json</code>, <code>london.json</code>, <code>praha.json</code>, <code>bogota.json</code></li>
</ul>
